<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label>Autor</label>
    <%= form.select :author_id, Author.all.collect {|author| [author.name , author.id]}, {}, {class: 'form-control'} %>
  </div>

  <div class="form-group">
    <label>Categoria</label>
    <%= form.select :category_id, Category.all.collect {|category| [category.description , category.id]}, {}, {class: 'form-control'} %>
  </div>

  <div class="form-group">
    <label>Texto</label>
    <%= form.text_area :text, id: :post_text, class: 'form-control' %>
  </div>

  <div class="form-group">
    <label>Assunto</label>
    <%= form.text_field :subject, id: :post_subject, class: 'form-control' %>
  </div>

  <div class="form-group">
    <label>Título</label>
    <%= form.text_field :title, id: :post_title, class: 'form-control' %>
  </div>

  <div class="actions">
    <input class="btn btn-success" name="commit" value="Enviar" data-disable-with="Enviar" type="submit">
  </div>
<% end %>
